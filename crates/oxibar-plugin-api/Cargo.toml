[package]
name = "oxibar-plugin-api"
version = "0.1.0"
edition = "2024"
description = "Shared plugin ABI for oxibar"
license = "GPL-3.0-only"

[dependencies]
iced = { version = "0.14.0", features = ["advanced", "tokio"] }
toml = "0.9.8"
